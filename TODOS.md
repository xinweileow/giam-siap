# TODOS

## Contracts

### Multi-vendor VendorRegistry

**What:** Generalize `VendorRegistry.trusted_pubkey` (currently one key) into a `Table<address, vector<u8>>` supporting multiple registered vendors, with each order referencing which vendor(s) it trusts.

**Why:** The MVP hardcodes one demo vendor's signing key. A real product would have many suppliers, each needing their own signing identity — the research doc's supplier story assumes exactly this.

**Context:** Surfaced during the `/plan-eng-review` pass on 2026-09-03. Today's single-pubkey design (see IMPLEMENTATION_PLAN.md §3, adopted to close a price/payee trust gap the review found) is intentionally the simplest version that still closes that gap for one vendor. This is the scaling path once there's more than one.

**Effort:** M
**Priority:** P3
**Depends on:** The signed-vendor-response scheme (§3, §4.2) already being solid in production use.

### On-chain OrderRegistry (scaling upgrade for order discovery)

**What:** Replace or augment the event-derived `getActiveOrders` tool (§4.2) with a shared `Table<ID, u8>` registry object, updated by every `create_order`/`execute_order`/`cancel_order` call, giving O(1) order-status lookups instead of event-log replay.

**Why:** Event replay is the correct, boring choice at demo scale (no extra shared-object writes, no contention point), but degrades as order-history volume grows over the system's lifetime.

**Context:** Surfaced during the `/plan-eng-review` pass on 2026-09-03, discussing how the agent discovers which orders are currently `Locked`. The registry alternative was explicitly evaluated and rejected for the MVP — it adds a write to every contract function (a single shared-object bottleneck under concurrent settlement) for a benefit this MVP's actual order volume doesn't need yet. Don't build speculatively — revisit only if real usage shows event-replay is actually too slow.

**Effort:** M
**Priority:** P4
**Depends on:** None — but should wait for real usage data before starting.

## Dashboard

### Live "current vendor price" column for still-Locked orders

**What:** The Live Order Table's "Current" column (§5.2) shows `"Monitoring…"` for a `Locked` order today, because nothing on-chain stores the vendor's last-checked price — `ProcurementOrder` (§3) has no such field, and the price only transiently exists inside the watcher process (`agent/watcher`) between `checkVendorPrice()` calls. To show a live number here, the watcher needs to publish its last-checked price somewhere the dashboard can read — e.g. a dynamic field on the order object, a small shared object it writes to, or (simplest, no new on-chain writes) the watcher and dashboard sharing a small in-memory/pub-sub layer if they ever run in the same process.

**Why:** The §5.2 mockup depicts this column with a live number for a monitoring order; the dashboard as built (2026-09-03) is honest about not having it rather than inventing a second vendor-polling path that would duplicate the watcher's responsibility and drift from §5.1/§5.3's "no vendor scraping in the dashboard" scope.

**Context:** Surfaced while building the dashboard (§7 step 5) on 2026-09-03 — see IMPLEMENTATION_PLAN.md's "Current status" section for the full reasoning.

**Effort:** M (needs an on-chain or cross-process design decision first, not just a dashboard change)
**Priority:** P3 — cosmetic gap, doesn't block the demo's core loop (§0.4).
**Depends on:** A decision on how the watcher should expose its last-checked price (not yet made).

## Infrastructure

### CI pipeline for the automated test suite

**What:** A GitHub Actions workflow running `sui move test` and the Vitest tool-test suite (§10.1, §10.2) on every push, plus the Playwright E2E test (§10.3) at minimum before any commit tagged demo-ready.

**Why:** §10 specifies what tests to write, not how they run automatically. Without CI, a regression is only caught if someone remembers to run the suite by hand before each change.

**Context:** Surfaced during the `/plan-eng-review` pass on 2026-09-03 as a distribution/CI gap — the review's own process requires flagging this explicitly rather than letting it drop silently. A Move-tests-only workflow is cheap and catches the highest-value regression class (contract bugs) immediately; the E2E job is harder to run in CI given testnet/faucet rate limits and may be better kept as a manual pre-demo gate for now.

**Effort:** S (Move-tests-only workflow) / M (full suite including E2E in CI)
**Priority:** P2
**Depends on:** §10's test suite existing first.

## Completed

- **`/auth` checks balance before requesting faucet funds** — 2026-09-05. The auto-faucet call fired unconditionally on every sign-in regardless of existing balance, so a returning owner who was already well-funded still saw the (accurate, per the faucet-honesty fix below) "funding failed" message any time the public faucet happened to be rate-limited — confusing, since they never needed the coins. New `GET /api/balance?address=` route (thin wrapper over `SuiGrpcClient.getBalance`) is checked first; the faucet is only called when balance is below 0.01 SUI. Also shows an explicit "this address already has enough testnet SUI" message when the check skips the faucet, rather than just going quiet. If the balance check itself fails, falls back to requesting the faucet anyway (unknown status — requesting funds for an already-funded address is harmless either way). Verified: new route returns the correct real balance for the test owner address; `npm run build` clean; `/auth` loads with no console errors.
- **Fixed a real gas-selection dead-end for freshly-funded owner addresses** — 2026-09-05. The first genuine end-to-end attempt (real Google login, real address) failed at `createOrder` itself with "insufficient SUI balance... to satisfy required budget" — nothing to do with the escrow amount. Root cause, found by reading `@mysten/sui`'s `CoinWithBalance` intent resolver directly: `coinWithBalance({useGasCoin:false})` (the fix from the gas-sponsorship item below) resolves by sweeping **every** coin the owner owns in the first page of `listCoins` into the payment split — it doesn't stop once it has "enough" — leaving nothing eligible for the separate, mandatory gas-selection step a full `tx.build({client})` also requires. Sending the address more separate coins didn't help (verified twice, empirically) — confirmed by direct inspection this isn't a balance or coin-count problem, it's structural: any freshly-funded real address hits this the moment it has ever received coins from a normal wallet/faucet flow. Fixed properly: `createOrder`/`cancelOrder` (`agent/sui-mcp/src/server.ts`) now build transaction-**kind**-only bytes from the start (`tx.build({client, onlyTransactionKind:true})`), skipping gas selection entirely — correct anyway, since real gas only ever comes from Enoki sponsorship now, never the owner's own coins. `dashboard/src/app/api/sponsor-transaction/route.ts` was simplified to match: it no longer reconstructs a full transaction and re-derives kind-only bytes (the stored bytes already are kind-only) — it just loads them via `Transaction.fromKind()` to introspect the Move-call allow-list, and ships the same bytes to Enoki unchanged. `devSignAndSubmit.ts` (the dev-only fallback, which bypasses sponsorship and needs a genuinely complete transaction) was updated to reconstruct via `Transaction.fromKind()` + `setSender` + a real full build before signing. One real SDK trap hit and fixed along the way: `Transaction.from()` only parses **full** `TransactionData` bytes and throws a BCS parse error on kind-only input — `Transaction.fromKind()` is the correct, distinct loader for `build({onlyTransactionKind:true})` output; confirmed by reading the SDK's own type declarations after a failed attempt assumed `.from()` handled both. Verified against the real owner address and the real Enoki API (not mocked): `createOrder`'s kind-only build succeeded, and `EnokiClient.createSponsoredTransaction` accepted the result and returned a real sponsorship digest. 19/19 sui-mcp Vitest tests pass (one rewritten to match the `fromKind` reconstruction), both `npm run build`s (sui-mcp, dashboard) clean.
- **Fixed an uncaught-exception 500 in `/api/sponsor-transaction`** — 2026-09-05. A QA pass (spawned right after the gas-sponsorship work below landed) found that `Transaction.from(fullBytes)` and `tx.build({client, onlyTransactionKind: true})` ran outside the route's only `try/catch` — a malformed or corrupt `unsignedTxBytesBase64` on any pending-tx record (not just a deliberately-bad test payload) crashed the route with an empty-body HTTP 500 instead of a real error. Fixed by wrapping the reconstruction step in its own `try/catch`, returning a `400` with the actual SDK error message. Verified by replaying the exact request that crashed it before the fix — now returns `{"error": "Stored transaction bytes for this pending tx are invalid or unbuildable: ..."}` at `400`.
- **Real Enoki gas sponsorship wired for `/sign`, closing §4.5's "owner never touches gas" promise** — 2026-09-05. Two new dashboard routes (`POST /api/sponsor-transaction`, `POST /api/sponsor-transaction/execute`) and a new `dashboard/src/lib/enokiServer.ts` holding the private `ENOKI_SECRET_KEY` (added to `dashboard/.env`, server-only, never `NEXT_PUBLIC_`) call Enoki's real `createSponsoredTransaction`/`executeSponsoredTransaction`. `/sign/page.tsx`'s `signAndSubmit` now signs the *sponsored* bytes Enoki returns rather than submitting the owner's own fauceted-gas transaction directly — Enoki's gas station pays network gas, the owner's `EnokiKeypair` only signs. A real, would-have-been-a-production-bug was caught and fixed first: `agent/sui-mcp/src/tools/createOrder.ts` built the escrow payment coin via `tx.splitCoins(tx.gas, ...)`, which under sponsorship would have split the payment off the *sponsor's* gas coin instead of the owner's balance — fixed via `coinWithBalance({balance, useGasCoin: false})`. A real, previously-undocumented Enoki API constraint was found by smoke-testing the live endpoint directly: it refuses to sponsor a Move call target that isn't explicitly allow-listed per-request (`allowedMoveCallTargets`) or pre-registered in the Portal — fixed by deriving the allow-list from the transaction's own `MoveCall` commands. Verified live against Enoki's real API (using the dev stand-in address as sender, no JWT needed) after also fixing a too-small test escrow amount that correctly tripped the contract's own `E_INSUFFICIENT_ESCROW` during Enoki's pre-sponsorship dry run. 19/19 sui-mcp Vitest tests and both `npm run build`s (sui-mcp, dashboard) still pass; 14/14 watcher tests unaffected. Not verified this session (stated honestly, not a code gap): the full HTTP round-trip from a registered pending-tx and `executeSponsoredTransaction` both need either a funded test address (the dev stand-in is too low on gas and the public faucet was rate-limited twice) or a real signed-in Google/zkLogin session to complete. See IMPLEMENTATION_PLAN.md's "Current status" for the full writeup.
- **`/auth`'s faucet-failure message no longer lies about success** — 2026-09-05. `finishSignIn` called `/api/faucet` and discarded the result with `.catch(() => {})`, so the owner always saw "a small amount of testnet SUI has been requested for you" even when the faucet actually failed (a real rate-limit `429` was hit this session) or was never called at all (no `telegramUserId`). Fixed: the response is checked, an honest failure message is shown on error, and the success message is now gated on a request having actually been attempted.
- **Hermes uses the owner's real address; `/auth`+`/sign` unified into one flow** — 2026-09-05. Testing the real signing flow (below) immediately surfaced that Hermes still hardcoded the dev stand-in address as every order's owner, which would make `/sign` refuse every real approval (it checks the signed-in address matches the order's recorded owner). Fixed with a new `getOwnerAddress` sui-mcp tool (reads the already-built `/api/owner-session` lookup) that Hermes now calls before every `createOrder`/`cancelOrder`; if it's null, Hermes sends the owner an `/auth` link instead of guessing an address. 3 new Vitest tests (19/19 pass, up from 16). Separately unified the page flow: `/sign` now auto-redirects to `/auth?returnTo=<itself>` when no browser session exists (instead of a dead-end message), and `/auth` shows the signed-in owner's own orders when there's nothing pending to approve, instead of a bare "you're signed in" screen. See IMPLEMENTATION_PLAN.md's "Current status" for full details and what's verified vs. not (the OAuth round-trip itself needs a real Google account to complete).
- **Real zkLogin browser-signing wired live** (§7 step 6's remaining step) — 2026-09-05. Real Enoki credentials (public API key, Google OAuth client id) now exist in `dashboard/.env`, and `/auth` was verified end-to-end (Google login → real zkLogin Sui testnet address → auto-faucet). With credentials live, `agent/sui-mcp` got a new `requestOwnerSignature` tool (`src/tools/requestOwnerSignature.ts`, 2 new Vitest tests — 16/16 pass, up from 14) that POSTs `createOrder`/`cancelOrder`'s unsigned tx bytes to the dashboard's `POST /api/pending-tx` and returns a `/sign?tx=<id>` link, instead of the dev-signer bridge signing immediately. `agent/hermes.config/system-prompt.md` now tells Hermes to call `requestOwnerSignature` and send the owner that link as the default path — synced live to the `giam-siap` Hermes profile via `sync-to-hermes-profile.mjs`. `devSignAndSubmitTx` was **not deleted** (TODOS.md originally said to) — kept as an explicitly-labeled, opt-in-only fallback (its tool description now says so directly) rather than removed outright, since a working fallback is cheap insurance per §9.2's demo-day philosophy and can be deleted later once the real flow is proven across more runs. Still open: gas sponsorship (see the new item above) — `/sign` still submits with the owner's own fauceted gas, not Enoki-sponsored gas, so §4.5's "owner never touches gas" promise is only half-closed.
- **Switched the demo currency from USD to Malaysian Ringgit (RM/MYR)** — 2026-09-04. This system is being built for use in Malaysia, and the one real mock-vendor site that exists (`cooking-bistro.vercel.app`) already prices in RM, not USD — the earlier USD convention was a mismatch waiting to confuse a demo. Relabeling only, no logic change: `target_price`/`price_cents`/`targetPriceCents`/`rate_mist_per_cent` all keep their exact field/param names (already shared with teammates as the vendor interface spec, §4.2) — "cents" now means **sen** (1 RM = 100 sen) everywhere, and `rate_mist_per_cent`'s value is unchanged (it was always an arbitrary demo rate, not a real exchange rate). Updated: `contracts/sources/procurement.move`'s doc comments, `agent/hermes.config/system-prompt.md` (Hermes now parses "RM10/kg" phrasing and is told this project prices in Ringgit, not USD — re-synced live to the `giam-siap` Hermes profile), `dashboard/src/lib/format.ts`'s `formatCentsAsUsd` → `formatCentsAsMyr` (and its one call site, `LiveOrderTable.tsx`), `dashboard/src/lib/store.ts`'s two `$`-formatted tx-log summary strings, `dashboard/src/lib/types.ts`'s doc comments, and `IMPLEMENTATION_PLAN.md`'s living-spec sections (§0's pitch/user stories, §3's contract description, §4.2/§4.4's tool descriptions, §5.2's mockup, §6's diagram, §8.3's checklist) — its dated "Current status" log entries were deliberately left as-is, since they're an accurate historical record of what was actually tested at the time (in USD phrasing). See `IMPLEMENTATION_PLAN.md`'s "Second terminology note" (§0.6) for the full reasoning. All 14 sui-mcp Vitest tests and both `npm run build`s (sui-mcp, dashboard) still pass.
- **`agent/hermes.config/` built and synced live** (§4.1, top P0 item) — 2026-09-04. `context.json` (canonical facts) + `system-prompt.md` (template) + `sync-to-hermes-profile.mjs` render into the `giam-siap` Hermes profile's `gateway-config.yaml` channel-override system prompt — the mechanism confirmed to actually work, since it's what a repo-local `AGENTS.md` can't guarantee for a gateway conversation (profiles run outside this repo's cwd). Item table expanded from 1 to 3 items (`coffee`, `sugar`, `rice`); `coffee` also carries a `displayUrl` for teammates' real `cooking-bistro.vercel.app` mock vendor site (confirmed live, but display-only — no signed `/api/price` endpoint yet, see below). Repo-root `AGENTS.md` added for `hermes -z`/CLI sessions. See IMPLEMENTATION_PLAN.md's "Current status" for full details.
- **Dev-only signer bridge for `createOrder`/`cancelOrder`'s unsigned output** (§7 steps 3-5's stand-in signer) — 2026-09-04. `agent/sui-mcp/src/tools/devSignAndSubmit.ts` + a new `devSignAndSubmitTx` MCP tool signs with the dev stand-in keypair and submits immediately; `system-prompt.md` now has Hermes call it right after `createOrder`/`cancelOrder`, while telling the owner plainly it's a temporary stand-in. A live Telegram order request can now actually lock funds on-chain end-to-end, not just build an unsigned tx. 1 new Vitest test.
- **Watcher's `checkNow()`/`onAlert` wired to real Telegram** (§4.3 point 2, §9.1) — 2026-09-04. `agent/watcher/src/httpServer.ts` exposes a loopback-only `POST /check-now`; `system-prompt.md` tells Hermes to `curl` it on a "check now" request. `agent/watcher/src/telegram.ts` shells out to `hermes send --to telegram` (no LLM, no running gateway required) against the `giam-siap` profile for both `onAlert` and a new settlement-confirmation message (with Suiscan link) on every `executed` outcome. 2 new Vitest tests.
- **zkLogin (Enoki) scaffolded** (§4.5, §7 step 6) — 2026-09-04. `dashboard/src/app/auth` + `dashboard/src/app/sign` pages, `dashboard/src/lib/ownerSessions.ts` + `pendingTx.ts` (JSON-file-backed, survive restarts), and `/api/owner-session`, `/api/faucet`, `/api/pending-tx[/[id]]` routes — all real code, untestable until real `ENOKI_API_KEY`/Google OAuth credentials exist (external, only the project owner can create them). Deliberately uses `@mysten/enoki`'s `EnokiFlow`/`EnokiKeypair` directly rather than `registerEnokiWallets` + `@mysten/dapp-kit`, since installed `dapp-kit@1.1.17` is still built on the deprecated JSON-RPC `SuiClient` this project migrated off of everywhere else. Not wired live — see the new "Wire sui-mcp's createOrder/cancelOrder to the dashboard's /sign flow" item above for what's left.
- **sui-tools MCP server connected to Hermes, and NL intent parsing validated live** (§7 step 3, first half) — 2026-09-03. `hermes mcp add sui-tools --command node --args <abs path>\agent\sui-mcp\dist\server.js` registered and connects cleanly (`hermes mcp test sui-tools`: 6/6 tools discovered). Confirmed Telegram/Discord/WhatsApp gateway credentials and the gateway service already existed on this machine from prior setup, so that sub-step of §7 step 3 needed no work. Validated §8.3's first checklist item for real: ran the plan's exact demo phrasing through `hermes -z` (oneshot) 5 times in a row, every run producing the identical correct `createOrder` call (`itemId=coffee, targetPriceCents=1000, quantity=50, paymentAmountMist=50000000`) and a successfully-built unsigned tx. Along the way, added self-loading `.env` support (via `dotenv`, resolved relative to each package's own source location) to both `agent/sui-mcp/src/config.ts` and `agent/watcher/src/config.ts` — without it, `hermes mcp add`'s spawned `node dist/server.js` subprocess had no env vars at all. Two real gaps found by this test and **not yet closed** — see the two new P0 items above: Hermes has no persistent, automatic source for the owner address/rate/vendor-URL context this test fed it by hand, and nothing in the Hermes-triggered path can actually sign the unsigned tx `createOrder` returns.
- **Dashboard built** (§5, §7 step 5) — 2026-09-03. Next.js (App Router) app in `dashboard/` reading the same on-chain shapes as `agent/sui-mcp` (§5's brief), using `SuiGrpcClient`/`listEvents` from the start (no `queryEvents`/old `SuiClient` ever touched). Single read-only page, exactly §5.2's three elements (Escrow Vault Tracker, Live Order Table, Transaction Log with Suiscan links), nothing from §5.3's exclusion list. `src/lib/store.ts` is an in-memory, cursor-paginated, append-only event store (one `emitModule`-filtered `listEvents` call covers all three event types) plus a `getObjects` refresh for still-`Locked` orders (object reads catch a status flip before the event stream does). `/api/state` route handler serves it; the client polls every 5s and shows a "Reconnecting…" indicator instead of freezing if a poll fails (§9.1) — verified manually by pointing `SUI_RPC_URL` at an unreachable host. Verified against the live deployed contract: correctly read 4 real orders from earlier `e2e-smoke.ts`/watcher runs, and the vault tracker's totals reconciled exactly against the on-chain math (§8.5's checklist passes). One known, documented gap: the "Current" price column can't show a live vendor price for a still-`Locked` order (nothing on-chain stores one) — see the new TODOS.md item above. See IMPLEMENTATION_PLAN.md's "Current status" section for the full writeup.
- **Deterministic watcher process built** (§4.1, §4.3, §7 step 4) — 2026-09-03. `agent/watcher/src/index.ts` runs a plain `setInterval` loop (`getActiveOrders()` → `getOrder()` → `checkVendorPrice()` → `executeOrder()`, no LLM) against the same sui-mcp tool functions Hermes uses, imported as a real local package dependency (`agent/sui-mcp` now emits type declarations; `agent/watcher` depends on `"@giam-siap/sui-mcp": "file:../sui-mcp"`). Core tick logic (`agent/watcher/src/loop.ts`) is dependency-injected and covered by 12/12 passing Vitest tests exercising the full §9.1 failure matrix (vendor failures never treated as price=0, alert-after-N-consecutive-failures, retry-with-backoff on transient `executeOrder` failures, race-vs-bug detection on an on-chain revert, one bad order never blocking the rest of a tick). Also added `agent/watcher/dev/vendor-stub.ts`, a local signed-quote HTTP server matching §4.2's vendor interface spec exactly, for testing the loop before teammates' real mock-vendor site exists. Not yet done: wiring `checkNow()`/`onAlert` to actual Telegram (see the new P1 item above) — that's blocked on §7 step 3, not on this work.
- **sui-mcp tool test suite fully green (13/13)** — 2026-09-03. Fixed both the fake-address fixtures and a deeper bug: tests asserted a `.target` field on `MoveCall` commands that the SDK doesn't produce (it splits into `.package`/`.module`/`.function`), and `createOrder`'s test expected 6 arguments against the real 7-argument contract signature (missing the `Clock` param). See IMPLEMENTATION_PLAN.md's "Current status" section.
- **Contract deployed to Sui testnet**, `VendorRegistry` configured (rate + dev vendor pubkey), wallet funded — 2026-09-03.
- **`@mysten/sui` upgraded 1.28.0 → 2.29.0 and ported to the gRPC Core API** (`SuiGrpcClient` replacing the removed `SuiClient`) — 2026-09-03. The public testnet fullnode had removed a JSON-RPC method the old SDK's transaction builder depended on; this was a hard, unconditional break, not an edge case. See IMPLEMENTATION_PLAN.md's "Current status" section for the full list of what this touched.
- **Full `create_order → execute_order` loop confirmed live on testnet** (real escrow lock, real signature verification, real payout to a fresh address) via `agent/sui-mcp/e2e-smoke.ts` — 2026-09-03.
