SUI_RPC_URL=https://fullnode.testnet.sui.io:443
SUI_PACKAGE_ID=0x...
SUI_AGENT_CAP_ID=0x...
SUI_VENDOR_REGISTRY_ID=0x...
SUI_CLOCK_ID=0x6
AGENT_PRIVATE_KEY=suiprivkey1...

# Dev-only mock vendor keypair for a local checkVendorPrice stub, until the real
# mock-vendor-site key is registered (see IMPLEMENTATION_PLAN.md §4.2/§7 step 2b).
DEV_VENDOR_PRIVATE_KEY=suiprivkey1...
DEV_VENDOR_PUBKEY_HEX=...

# Where the dashboard's /api/pending-tx + /sign live (§4.5 step 3, §7 step 6). Defaults to
# http://localhost:3000 if unset — only override for a non-local dashboard deployment.
# DASHBOARD_URL=http://localhost:3000
